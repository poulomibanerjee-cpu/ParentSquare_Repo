# runtime working copy + generated data (regenerated by `node seed.cjs` / first run)
data/

# Capacitor native projects (created by `npx cap add ios` / `android`)
ios/
android/

# dependencies
node_modules/

# OS / editor / logs
.DS_Store
*.log
